git add combat_fiction36.db &&
git commit -m '36' && git push &&

git add combat_fiction37.db &&
git commit -m '36' && git push &&

git add combat_fiction38.db &&
git commit -m '36' && git push &&

git add combat_fiction39.db &&
git commit -m '36' && git push &&

git add combat_fiction40.db &&
git commit -m '36' && git push &&

git add combat_fiction41.db &&
git commit -m '41' && git push
